# PostgreSQL Database Configuration
POSTGRES_DB=${POSTGRES_DB}
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_PORT=${POSTGRES_PORT}

# Database Connection URL (for applications)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Ollama AI Configuration
# llama3.2:1b = 2x faster (1GB RAM, ~7-10s inference)
# llama3.2:3b = balanced (2.2GB RAM, ~14-20s inference)
# phi3:mini = best quality (2.3GB RAM, similar speed to 3b)
OLLAMA_MODEL=llama3.2:1b

# FastAPI Backend Configuration
FASTAPI_PORT=${FASTAPI_PORT}
TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}

# Data Path (bind mounts en servidor)
DATA_PATH=/var/lib/mispesos
