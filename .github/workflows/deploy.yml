name: Deploy to alvis-server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show deployed files
        run: |
          echo "Repository cloned successfully"
          ls -la
          echo "Workflow is ready for deployment commands"
